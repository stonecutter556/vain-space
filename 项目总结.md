# Vain Space — 项目总结

> 从零开始的全栈个人时间管理平台

---

## 一、技术栈

```
后端：Java 17 + Spring Boot 3 + JPA + MySQL 8.4 + JWT
前端：Vue 3 + Vite + Naive UI + Pinia + Axios
部署：GitHub + Railway（自动 CI/CD）
工具：IntelliJ IDEA + Maven + Git
```

---

## 二、环境搭建

| 软件 | 版本 | 用途 |
|:----|:----:|:-----|
| JDK | 17 | Java 运行环境 |
| MySQL | 8.4 | 数据库 |
| Node.js | 20 | 前端运行环境 |
| Maven | IDEA 内置 | Java 构建工具 |
| Git | 2.54 | 版本控制 |

---

## 三、后端模块（6个）

| 模块 | API 端点 | 说明 |
|:----|:---------|:-----|
| 目标管理 | `/api/goals` | 长期目标 + 今日计划清单，含 type/done/estimatedHours/completedHours |
| 文章/日记 | `/api/articles` | 支持日记/blog 两种类型 |
| 每日进度 | `/api/daily-logs` | 每日记录心情、番茄数 |
| 健身记录 | `/api/workout-logs` | 记录动作、组数、次数、重量 |
| 番茄钟 | `/api/pomodoro` | 计时 + 关联目标 + 统计 API |
| 书签管理 | `/api/bookmarks` | 收藏链接，分类管理 |

### 扩展模块

| 模块 | API 端点 | 说明 |
|:----|:---------|:-----|
| 用户认证 | `/api/auth` | 注册/登录，JWT Token |
| 用户管理 | `/api/user` | 个人信息、头像上传、搜索、公开主页 |
| 消息系统 | `/api/messages` | 用户私聊、会话列表、未读数 |
| 管理后台 | `/api/admin` | 用户列表、角色管理 |

---

## 四、前端页面（10个）

| 页面 | 路由 | 功能 |
|:----|:-----|:-----|
| 首页 | `/` | Widget 网格布局（时钟/统计/文章/健身/进度点阵） |
| 目标 | `/goals` | 今日计划（清单勾选）+ 长期目标（时长追踪） |
| 文章 | `/articles` | 文章/日记列表，含公开开关 |
| 健身 | `/workout` | 健身记录 CRUD |
| 番茄钟 | `/pomodoro` | 计时器 + 关联目标 + 饼图+折线图统计 |
| 书签 | `/bookmarks` | 收藏链接管理 |
| 发现 | `/discover` | 用户搜索 + 公开文章广场 |
| 登录 | `/login` | 登录/注册 |
| 个人中心 | `/profile` | 头像上传、昵称、简介、邮箱 |
| 管理后台 | `/admin` | 管理员用户管理 |

### 消息系统

| 页面 | 路由 | 功能 |
|:----|:-----|:-----|
| 消息列表 | `/messages` | 所有会话、未读标记 |
| 聊天 | `/messages/:userId` | 实时对话 |
| 用户主页 | `/users/:id` | 查看他人公开信息+发私信 |

---

## 五、视觉风格迭代

```
第1版：深色科技风（蓝紫色渐变、毛玻璃导航）
第2版：3D 布料物理模拟（Three.js，后已移除）
第3版：Widget 壁纸风格（当前版本）
       └─ 背景壁纸 + 自定义字体（Urbanist/Simplon/Nothing/Mgen）
       └─ 半透明黑底卡片 + 白色细边框 + 10%圆角
       └─ 极简白色系文字（不同透明度）
       └─ 全局统一风格（所有页面一致）
```

---

## 六、核心功能

| 功能 | 说明 |
|:----|:------|
| 用户认证 | JWT 登录/注册，路由守卫 |
| 数据隔离 | 每人只能看到自己的数据 |
| 公开/私有 | 目标/文章可选择公开，展示在发现页 |
| 用户搜索 | 按用户名搜索，查看公开主页 |
| 私聊系统 | 用户间实时消息 |
| 管理员后台 | 用户角色管理 |
| 番茄统计 | 饼图（各目标占比）+ 折线图（时段分布） |
| 目标时长 | 预计时长 + 已完成时长（番茄钟自动同步） |
| 今日计划 | 清单式任务管理，勾选完成 |
| Widget 首页 | 可拖拽小组件（时钟/统计/进度点阵等），双击展开 |

---

## 七、部署上线

```
前端地址：https://vain-space-production.up.railway.app
后端地址：https://lucky-enchantment-production-ef24.up.railway.app
自定义域名：vainlhy.top（待配 Cloudflare）
```

### 开发上线流程

```bash
# 本地修改代码
# 测试没问题后：
git add .
git commit -m "改了什么"
git push

# Railway 自动检测到推送
# 自动构建部署（1-2分钟）
# 刷新网站即可看到最新版本
```

---

## 八、数据库表（7张）

| 表名 | 说明 |
|:-----|:------|
| `users` | 用户（含 role/nickname/avatar/bio） |
| `goals` | 目标/任务（含 type/done/estimatedHours/completedHours） |
| `articles` | 文章/日记 |
| `daily_logs` | 每日进度 |
| `workout_logs` | 健身记录 |
| `pomodoro_sessions` | 番茄钟（含 goalId 关联目标） |
| `bookmarks` | 书签 |
| `messages` | 私聊消息 |

---

## 九、已解决问题记录

| 问题 | 解决方案 |
|:----|:---------|
| API 路径 404 | 修正 axios baseURL 拼接 `/api` |
| 前后端跨域 | 添加 CORS 配置 |
| 容器休眠超时 | 关闭 Serverless 模式 |
| 番茄钟切屏不准 | 改为基于 Date.now() 真实时间计算 |
| 健身表单校验误报 | 移除 n-input-number 的 required 规则 |
| Git push 连接失败 | 配置 HTTP 代理 |
| node_modules 误提交 | git rm --cached 移除 + .gitignore |

---

## 十、后续可扩展方向

| 方向 | 难度 | 说明 |
|:----|:----:|:-----|
| PWA 手机安装 | ⭐ | 加 manifest.json，手机桌面图标 |
| 数据导出 | ⭐⭐ | 导出为 JSON/CSV 备份 |
| 数据统计仪表盘 | ⭐⭐ | 更丰富的图表（ECharts） |
| 暗色/亮色主题切换 | ⭐⭐ | 主题变量切换 |
| Electron 桌面端 | ⭐⭐ | 包装为 Windows/Mac 应用 |
| 邮件通知 | ⭐⭐⭐ | 注册验证、每日总结邮件 |
| 文件/图片上传 | ⭐⭐⭐ | 文章配图、更多文件类型 |

---

*项目创建时间：2026年5月*
*最后更新：2026年5月*
